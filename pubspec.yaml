name: form_builder_multi_dropdown
description: "Form Builder Multi Dropdown"
version: 0.9.0
resolution: workspace
homepage: https://flutterartist.dev
repository: https://github.com/o7planning/form_builder_multi_dropdown

environment:
  sdk: ^3.7.2
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_form_builder: ^10.3.0 # ^10.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
